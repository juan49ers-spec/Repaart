}
            {count > 0 && (
                <span className={`px-2 py-0.5 rounded-full text-[10px] ${active ? 'bg-slate-900 text-white' : 'bg-slate-100 text-slate-500'}`}>
                    {count
